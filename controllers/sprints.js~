'use strict';

var events = require('../models/events');
/**
 * Controller that renders our sprint report 3
 */
function sprint3 (request, response) {
  var contextData = {
  };
  response.render('sprint3.html', contextData);
}

/**
 * Controller that renders our sprint report 4
 */
function sprint4 (request, response) {
  var contextData = {
  };
  response.render('sprint4.html', contextData);
}

/**
 * Controller that renders our sprint report 5
 */
function sprint5 (request, response) {
  var contextData = {
  };
  response.render('sprint5.html', contextData);
}

/**
 * Controller that renders our sprint report 6
 */
function sprint6 (request, response) {
  var contextData = {
  };
  response.render('sprint6.html', contextData);
}

/**
 * Controller that renders our final report
 */
function sprint_final (request, response) {
  var contextData = {
  };
  response.render('finalreport.html', contextData);
}

module.exports = {
    'sprint3':sprint3
    'sprint4':sprint4
    'sprint5':sprint5
    'sprint6':sprint6
    'sprint_final':sprint_final
};
